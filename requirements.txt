# Python dependencies for the time-series TDA notebook
# Install with: pip install -r requirements.txt

numpy>=1.24
matplotlib>=3.7
ripser>=0.6       # Vietoris-Rips persistent homology (same Ripser backend as TDAstats/R)
jupyter>=1.0      # Jupyter notebook environment

# For funTDA/analysis/funTDA.ipynb (Part A) — install separately, in its own
# environment if possible: giotto-tda pins an upper bound on numpy that can
# conflict with the numpy>=1.24 above. See requirements-partA.txt.
# pip install -r requirements-partA.txt
