# Python dependencies for funTDA/analysis/funTDA.ipynb (Part A) only.
# Kept separate from time-series/requirements.txt: not because giotto-tda
# itself needs numpy<2 (it only requires numpy>=1.19.1), but because it pins
# scikit-learn==1.3.2 exactly, which requires numpy<2.0,>=1.17.3 transitively.
# Install with: pip install -r funTDA/requirements.txt
#
# NOTE: the exact upper bounds below are a conservative estimate, not
# verified against a live package index in this environment (no network
# access at the time this was written). Confirm against the giotto-tda
# version you actually install — giotto-tda has historically required
# numpy<2 — and tighten/relax these pins accordingly before relying on them.

numpy<2.0
giotto-tda>=0.6,<0.7
networkx>=3.0,<4.0
